from logging import Logger
from typing import Any

import _typeshed

logger: Logger

class DeclarativeMetaclass(type):
    def __new__(
        cls: type[_typeshed.Self], name: str, bases: tuple[type[Any], ...], attrs: dict[str, Any]
    ) -> _typeshed.Self: ...

class ModelDeclarativeMetaclass(DeclarativeMetaclass):
    def __new__(
        cls: type[_typeshed.Self], name: str, bases: tuple[type[Any], ...], attrs: dict[str, Any]
    ) -> _typeshed.Self: ...
